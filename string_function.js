export const capitializeString = str => str.toUpperCase();

const capitializeString = (string) => string.charAt(0).toUpperCase + string.slice(1);
export { capitializeString };
export const foo = "bar";
export const bar = "foo";